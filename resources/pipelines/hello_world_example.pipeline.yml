resources:
  pipelines:
    hello_world_example_pipeline:
      name: hello_world_example_pipeline
      serverless: true
      catalog: forecasting_dev
      schema: learning
      libraries:
        - notebook:
            path: ../../src/examples/pipeline_main.py
      environment:
        dependencies:
          - -r ../../requirements/requirements-standard.txt
