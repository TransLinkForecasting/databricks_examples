resources:
  pipelines:
    standard_serverless_pipeline:
      name: standard_serverless_pipeline
      serverless: true
      catalog: forecasting_dev
      schema: learning
      libraries:
        - notebook:
            path: ../../src/examples/pipeline_main.py
      environment:
        dependencies:
          - -r ../../requirements/requirements-standard.txt
